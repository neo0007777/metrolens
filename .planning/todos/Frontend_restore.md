# Frontend Restore
- [X] Removed bloated `webapp` folder that was hallucinating results and causing errors.
- [X] Copied `webapp_old` to `frontend/`.
- [X] Ran `npm install` and started Next.js dev server on port 3000.
- [X] Mapped frontend API calls strictly to backend (`http://127.0.0.1:8000/api/v1/:path*`).
- [X] Adhered strictly to: "measure only using ml ,ai should only cross check with declarations".
